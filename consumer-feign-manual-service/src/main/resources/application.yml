server:
  port: 10010
spring:
  application:
    name: consumer-feign-manual-service #消费端 service 方式调用
#eureka配置
eureka:
  client:
#    fetch-registry: true
#    register-with-eureka: false
    service-url:
      defaultZone:  http://localhost:10001/eureka/
  instance:
    prefer-ip-address: true